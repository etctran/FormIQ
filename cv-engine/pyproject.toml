[build-system]
requires = ["scikit-build-core>=0.10", "pybind11>=2.12"]
build-backend = "scikit_build_core.build"

[project]
name = "cv-engine"
version = "0.1.0"
description = "FormIQ pose keypoint extraction (MediaPipe + OpenCV, pybind11 bindings)"
requires-python = ">=3.11"

[tool.scikit-build]
wheel.packages = []
cmake.build-type = "Release"
